import styles from '../styles/TeamsList.module.scss';

const TeamsList = () => (
    <section className={styles.teams}>
        <h2>Equipes Participantes</h2>
        <p>A lista de equipes será divulgada em breve. Fique ligado!</p>
    </section>
);

export default TeamsList;
